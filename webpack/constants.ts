import { resolve } from 'path';

export const DIST_PATH: string = resolve(process.cwd(), 'dist');
export const LOCALHOST: string = 'http://localhost';
export const PORT: number = 1337;
export const SRC_PATH: string = resolve(process.cwd(), 'src');
export const TITLE: string = `Kieran O'Neill`;
export const URI_LIMIT: number = 65000;
